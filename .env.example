# Copy to `.env` locally (never commit `.env`).
# On Render/hosting, set these in the dashboard instead.

# Required in production (generate: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
SECRET_KEY=django-insecure-_@_&q7j_b!5l57j!z+t0%0-f7q1c!3!g*p7*k+!f9y-qx4u1w

# false on Render/production
DEBUG=True

# Comma-separated, no spaces (or use spaces if you trim — we strip each host)
ALLOWED_HOSTS=127.0.0.1,localhost,collins-site.onrender.com

# Leave empty for local SQLite. On Render, paste Internal/External Database URL.
DATABASE_URL=

# Gmail (or other SMTP): use an App Password for Gmail, not your normal password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=kyerematengcollins93@gmail.com
EMAIL_HOST_PASSWORD=yrtifdqljohudbtw
DEFAULT_FROM_EMAIL=kyerematengcollins93@gmail.com

# Where contact-form submissions are delivered
CONTACT_RECIPIENT_EMAIL=you@example.com
